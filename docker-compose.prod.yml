# Production docker-compose override
# Used by setup.sh to create the working compose.yml
# Build context points to src/ subdirectory (cloned repo).

services:
  bridge:
    build:
      context: ./src # The src/ is auto-created symlink
      dockerfile: Dockerfile
    container_name: smarthome-bridge
    restart: unless-stopped
    environment:
      - TZ=Europe/Moscow
    volumes:
      - ./config.yaml:/config/config.yaml:ro
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
